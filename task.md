# משימות פיתוח: מנגנון אישור והשגות לשותפים

- [/] **שלב 1: עדכון מסד הנתונים (Context)**
  - [ ] הוספת סטטוס `pending` | `active` | `disputed` למודל `SpaceMember`.
  - [ ] הוספת שדה `disputeMessage`.
  - [ ] הגדרת ברירת מחדל לאורחים חדשים כ-`pending`.
  - [ ] יצירת פונקציית מעבר `migrateGuestToRealUser` להמרת אורח (Shadow Token) למשתמש רשום (Firebase UID).

- [ ] **שלב 2: ממשק משתמש לאורח (Guest UI)**
  - [ ] יצירת באנר `PendingApprovalBanner` שיוצג בראש הפרויקט עבור שותפים בסטטוס המתנה.
  - [ ] חלון קופץ להזנת טקסט השגה (Dispute Modal).
  - [ ] חומת הרשמה (Auth Wall Modal) המוקפצת כשלוחצים "מאשר שותפות".

- [ ] **שלב 3: ממשק מנהל (Admin UI)**
  - [ ] עדכון `PartnersWidget` להצגת סטטוס השותפים (ממתין, אישר, יש השגה).
  - [ ] הצגת הודעת ההשגה (Dispute) בצורה בולטת למנהל כדי שיוכל לטפל בה.
